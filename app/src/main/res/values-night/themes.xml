<resources>
    <!-- Base application theme. -->
    <style name="Theme.RehlatTask1" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="fontFamily">@font/poppins</item>
    </style>

    <style name="circleImageView" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>
    <style name="CustomTabText" parent="@android:style/TextAppearance.Widget.TabWidget">
        <!-- Customize text style properties as per your requirements -->
        <item name="android:textColor">@color/red</item>
        <item name="android:textSize">12sp</item>
        <item name="fontFamily">@font/poppins_semibold</item>
        <item name="textAllCaps">false</item>
        <!-- Add any other desired text style attributes -->
    </style>
    <style name="TabText" parent="@android:style/TextAppearance.Widget.TabWidget">
        <!-- Customize text style properties as per your requirements -->
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">12sp</item>
        <item name="fontFamily">@font/poppins_medium</item>
        <item name="textAllCaps">false</item>
        <!-- Add any other desired text style attributes -->
    </style>
</resources>